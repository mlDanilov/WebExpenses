@using WebExpenses.Models.Item;
@model MItemDDList
<select id="@ViewData["SelectItemId"]" name="@ViewData["SelectItemName"]">
    @{
        Html.RenderPartial("ItemOptions", Model);
    }
</select>

